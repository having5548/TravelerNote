# 更新日志

## v1.1.5 · versionCode 7（当前版本）

### 新增：底部「游戏资讯」页签

四个板块放在同一页里，顶部有刷新按钮（切页签 10 分钟内不重复请求，手动刷新不受限制）。

**一、进行中的活动**

- 数据来自官方战绩接口 `game_record/app/genshin/api/act_calendar`（对齐胡桃工具箱 `GameRecordClient.GetActCalendarAsync`：POST `role_id`/`server`，X4 盐 Gen2 且 body 参与签名）
- 每条显示：活动名、**开始 ~ 截止时间**（同年只显示月-日 时:分，跨年带年份）、状态（进行中 / 即将开始）、**剩余天数或小时**、可获得的奖励（`原石×420` 形式）
- 只列出尚未结束的活动，进行中的排在前面，按截止时间升序

**二、UP 池**

- 同一个接口里的 `avatar_card_pool_list` / `weapon_card_pool_list` / `mixed_card_pool_list`
- 每条显示：池子类型（角色活动祈愿 / 武器活动祈愿 / 集录祈愿）、池名、**开始 ~ 截止时间**、剩余时间，以及 UP 角色 / 武器的头像与名字（最多 6 个）

**三、游戏公告（与米哈游启动器同源）**

- 数据来自 `hk4e-ann-api.mihoyo.com` 的 `getAnnList` + `getAnnContent`（对齐胡桃工具箱 `AnnouncementService`），不需要登录
- 列表按官方分组展示标题、标签与起止时间；活动公告（`type_id = 1`）排最前且按截止时间升序，方便看快结束的活动
- **详情在应用内打开**（不跳外部浏览器）：新增 `AnnouncementActivity`，用 WebView 套一层与胡桃工具箱同款的模板 —— 图片自适应宽度、`rem` 字号放大到手机可读、深色模式把正文里的深色字反色、正文里的外链才交给系统浏览器
- 点开列表**立即进详情页**，正文在详情页后台拉取（成功替换、失败把原因写在正文位置），不会出现"点了没反应"或空白页
- 正文按需拉取（点击时取一次并缓存 30 分钟），进页签只下 30 KB 左右的列表

**四、原神官方 B 站内容**

- **官方投稿视频 + 专栏**：走客户端接口 `app.bilibili.com/x/v2/space`（appkey 签名，**无需登录**），拿到投稿视频（标题、封面、时长、播放量、发布时间）与专栏（标题、摘要、封面），按时间倒序混排。注意签名：query 必须把**所有参数含 `ts` 按 key 字母序**排列后再 `md5(query + appsecret)`，顺序不对服务端会返回 `-3 签名错误`
- **真实动态（图文）**：B 站已把空间动态接口 `polymer/web-dynamic/v1/feed/space` 加了风控 —— 不带登录态的请求一律 `-412 request was banned`（实测补 `buvid3` / `bili_ticket` / wbi 签名都无效）。因此改用**隐藏 WebView** 打开官方动态页，在页面脚本执行前 hook `fetch` / `XMLHttpRequest`，截下页面自己发出的那次 `feed/space` 响应（浏览器上下文里带着 B 站自己下发的风控 cookie），裁剪成小 JSON 后交回原生渲染；抓不到就只显示上面的投稿 + 专栏
- 内容**全部原生排版**，不打开 B 站网页：正文 / 标题、配图（最多 3 张，点击同样进客户端）、`视频 / 动态 / 专栏` 标签、发布时间与播放量
- **视频用哔哩哔哩客户端拉起**：优先用接口返回的官方深链（`bilibili://video/<aid>?cid=…`、`bilibili://article/<id>`），再依次退到 `bilibili://video/<aid>`、`bilibili://video/<bvid>`，**只有都没装客户端时才用浏览器打开网页版**并给出提示

---

## v1.1.4 · versionCode 6

### 一、界面与交互

- **社区（论坛）签到移到首页签到卡片内**：与游戏签到同一张卡片，「自动」徽标只属于游戏签到，社区签到依旧是**手动**按钮
- **全屏背景**：背景图（含磨砂 / 亚克力材质）铺满整屏并延伸到状态栏，「旅行工具」页签连状态栏一起覆盖，不再有一条突兀的纯色状态栏
- **背景材质重做**：对齐胡桃工具箱的窗口材质思路 —— **磨砂**只叠雾面不改清晰度，**亚克力**只做很轻的模糊（`1/(1+0.18·等级)`），不再把壁纸糊成一团；两种材质各自记忆等级
- 新增**首次启动引导页**：完整《免责声明》与《隐私政策》，底部要求手动输入「我已仔细阅读免责声明与隐私政策，并同意上述条款」才能进入

### 二、多账号与登录态

- **设置 → 账户**：显示当前账号、账号列表、可单独删除某个账号，**退出登录**入口移到这里
- **多账号并行缓存**：像胡桃工具箱一样同时缓存多个账号的 cookie（`cookie_token` / `ltoken` / `stoken`），各账号凭证彼此隔离、互不串号；设备标识全局共用，换账号不改设备身份
- **定时刷新**：前台启动时按 6 小时节流统一刷新所有账号的凭证，长期不打开也不容易失效
- 登录页新增「添加账号」，旧版单账号凭证自动迁移为第一个账号

### 三、我的角色改为官方接口

- 不再走第三方「提瓦特小助手」，直接请求米游社官方战绩接口 `game_record/app/genshin/api/character/list`，端点、DS 签名（X4 盐 Gen2、body 参与签名）、Cookie 组成、`x-rpc-*` 请求头全部对齐 [Snap.Hutao（胡桃工具箱）](https://github.com/DGP-Studio/Snap.Hutao) 的 `GameRecordClient`
- **设备指纹注册**：首次请求前先用 `device-fp/api/getFp` 注册设备指纹（缓存 7 天）。未注册的随机指纹几乎必然被风控判为异常环境（`5003`），这是此前拿不到角色数据的主因
- **1034 人机验证自动处理**：遇到 `需要人机验证` 时自动走 card 接口流程 `createVerification → 极验 → verifyVerification`，换回 `x-rpc-challenge` 后重放请求
- **失败原因直接显示在界面上**：区分 `5003` 风控、`1034` 人机验证、登录失效与其它 retcode + message（release 包会剥离 Log，看不到日志就只能靠界面）；角色卡新增「刷新角色数据」按钮
- **角色详情（面板 + 圣遗物）**：再请求一次官方 `character/detail`（body 带全部 `character_ids`），拿到并显示
  - **面板属性**：`selected_properties` → 生命值 / 攻击力 / 防御力 / 暴击率 / 暴击伤害 / 元素充能效率 / 元素精通 / 元素伤害加成等，显示总值并在括号里给出「基础+加成」；接口的百分比两种口径（`0.625` 与 `62.5`）都能正确显示
  - **圣遗物**：`relics` → 部位、套装、等级、星级、主词条与全部副词条（列表接口只给名字，且字段名是 `reliquaries`，与详情的 `relics` 不同）
  - **天赋等级**：`skills` → 普通攻击 / 元素战技 / 元素爆发
  - 卡片上显示「面板：…」与「圣遗物 N 件 · 套装×N」，点开角色弹窗看完整面板与每件圣遗物的主副词条；详情接口失败时会显示它自己的 retcode，同时仍然展示列表数据

### 四、构建

- **开启代码混淆**：R8 + `-repackageclasses` + 激进重载 + `assumenosideeffects` 抹掉 `Log`，35 个业务类被重命名
- release 仍固定用 `release.keystore` 签名，可直接覆盖旧版本安装

### 五、修复

- **引导页输入框被输入法遮挡**：输入法弹出时底部输入区整体上移（IME insets），输入内容不再被挡在键盘后面

---

## v1.1.3 · versionCode 5

### 新增：旅行工具页（底部第 4 个页签）

**一、原神战绩（原生拉取，不打开网页）**

- **深境螺旋**：本期最深层数、星数、战斗/胜场次数；按层显示 `第N层 X/9★` 与上阵角色头像
- **幻想真境剧诗**：难度、最高幕数、勋章数、幻剧之花；上阵与备选角色头像
- 接口路径、DS 签名（X4 盐 Gen2，且参与签名的 query 按字母序重排）、Cookie 组成、响应字段全部对齐 [Snap.Hutao（胡桃工具箱）](https://github.com/DGP-Studio/Snap.Hutao) 的 `GameRecordClient`
- 「刷新战绩」按钮手动刷新；失败时在状态行直接显示 `retcode` 与 `message` —— release 包会剥离 Log，把原因显示到界面上才便于反馈问题

**二、米游社官方网页工具入口**（点击用系统浏览器打开）

| 分组 | 入口 |
| --- | --- |
| 社区与签到 | 原神社区、每日签到（网页版）、米游社通行证 |
| 地图与图鉴 | 观测枢 · 提瓦特大地图、观测枢 · 旅行百科 |
| 数据与资讯 | 原神战绩页、祈愿记录、原神官网 |

8 条链接均实测返回 HTTP 200。刻意用系统浏览器打开：战绩、祈愿、通行证等页面需要浏览器里的米游社登录态，应用内 WebView 没有这份登录态。

---

## v1.1.2 · versionCode 4

- 补齐 **GPL-3.0** 许可证全文（FSF 官方文本）与免责声明，新增「许可证」章节
- 全部 19 个 Kotlin 源文件添加 SPDX 版权与许可证标识
- 本版**无功能改动**

## v1.1.1 · versionCode 3

- 新增**旅行日历**：首页按「本月第 N 次签到」列出每天的奖励，已领取 / 今日 / 未领取三态
- **游戏签到（原神 luna）保持自动执行**；**社区（论坛）签到改为首页手动按钮**，包含 1034 → 极验 → `verifyVerification` 换取 challenge 的完整流程
- 修复：
  - 「无感验证」开关原本完全无效（从未接通的死代码）
  - `Rsa` 复用同一个 `Cipher` 导致的线程安全问题
  - `DS.gen2` 中永不成立的死分支
  - 未登录时免责声明连弹两次
  - 旋转屏幕打断验证码流程（`configChanges`）
- 优化：前台恢复 60s 节流、背景图仅在来源变化时重新下载、清理社区签到相关的死代码
- 构建：补回 gradle wrapper，release 接入 `keystore.properties` 签名，产物自动复制到 `apk/`

## v1.1.0 · versionCode 2

- 首个公开版本：游戏签到、实时便签、我的角色、扫码 / 短信 / Cookie 登录、6 色主题、可选背景图

---

## 安装

- 要求 **Android 11（API 30）及以上**，从 Release 下载 `TravelerNote-v1.1.5.apk` 直接安装
- 签名固定（证书 SHA-256 `0ea8c2be…b04dc`），可直接覆盖安装旧版本，无需卸载
- 自行构建：需要 **JDK 17** 与 Android SDK 34，`.\gradlew.bat assembleRelease`；签名配置见 README

## 已知问题与计划

- 深境螺旋 / 幻想真境剧诗的 **1034 人机验证尚未自动处理**（「我的角色」已处理），遇到会显示 `retcode=1034`
- 「游戏资讯」里的**真实 B 站动态**依赖隐藏 WebView 渲染官方动态页来截获数据，页面改版或风控加强时可能抓不到；此时会自动退回到官方投稿与专栏（客户端接口，无需登录）
- **幽境危战**（`hard_challenge`）尚未接入
- 抽卡记录分析、养成计算等依赖本地游戏数据的能力未实现

## 免责声明

本项目为**非官方**第三方工具，与米哈游 / 上海米哈游影铁科技有限公司无任何关联，未获得任何形式的授权或认可，仅供个人学习与技术研究使用。自动签到可能违反米哈游服务条款，使用风险自负。许可证为 [GPL-3.0](LICENSE)，详见 README。
